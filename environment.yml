# create a conda env with 'conda env create -f environment.yml'
name: Foundation
channels:
  - conda-forge
dependencies:
  - python=3.10
  - scikit-learn
  - pytorch-lightning
  - pandas
  - scipy
  - pip
  - pip:
    - matplotlib
    - wandb